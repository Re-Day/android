<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReDay" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/main</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="colorControlActivated">@color/main</item> <!-- 커서 색상 -->
        <item name="android:windowLightStatusBar">true</item>   <!-- 상태바 아이콘(true: 검정 / false: 흰색) -->
        <item name="android:navigationBarColor">#B3FFFFFF</item> <!-- 하단 탐색바 배경 -->
    </style>

    <style name="Theme.ReDay" parent="Base.Theme.ReDay" />

    <style name="AppTheme.NumberPicker" parent="Base.Theme.ReDay">
        <item name="colorControlNormal">@color/transparent</item>
        <item name="fontFamily">@font/pretendard_semi_bold</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/main</item>
        <item name="android:unselectedAlpha">10</item>
    </style>
</resources>